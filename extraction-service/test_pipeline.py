import os, cv2; from pipeline.render import render_pdf_pages; from pipeline.grid import detect_grid_boxes; from pipeline.ocr import process_box; p = [f for f in os.listdir(r'C:\Users\kasim\Downloads') if 'MAR-75-WI' in f][0]; path = os.path.join(r'C:\Users\kasim\Downloads', p); print(path); imgs = render_pdf_pages(path, 300); boxes = detect_grid_boxes(imgs[2]); print(f'boxes={len(boxes)}');
