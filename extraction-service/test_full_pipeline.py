import os, sys, cv2; import asyncio; from pipeline.render import render_pdf_pages; from pipeline.header import extract_header_metadata; from pipeline.grid import detect_grid_boxes; from pipeline.ocr import process_box; p = [f for f in os.listdir(r'C:\Users\kasim\Downloads') if 'MAR-75' in f][0]; path = os.path.join(r'C:\Users\kasim\Downloads', p); print(f'Processing {path}'); imgs = render_pdf_pages(path, 150); metadata = extract_header_metadata(imgs[0]); print('META:', metadata); boxes = detect_grid_boxes(imgs[2]); print(f'Found {len(boxes)} boxes. Testing box 8:'); print(process_box(imgs[2], boxes[8], 1, 1, metadata.partNumber))
