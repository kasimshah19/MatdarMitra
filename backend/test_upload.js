const fs = require('fs'); const axios = require('axios'); const FormData = require('form-data'); const p = fs.readdirSync('C:\\Users\\kasim\\Downloads').find(f => f.includes('MAR-75')); const form = new FormData(); form.append('file', fs.createReadStream('C:\\Users\\kasim\\Downloads\\' + p)); axios.post('http://127.0.0.1:5000/api/upload', form, {headers: form.getHeaders(), timeout: 0}).then(r => console.log('OK', r.data)).catch(e => console.error('FAIL', e.response?.data||e.message))
